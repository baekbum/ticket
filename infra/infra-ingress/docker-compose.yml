services:
  ingress-nginx:
    image: nginx:latest
    container_name: ingress-nginx
    ports:
      - "80:80"
    volumes:
      - ./default.conf:/etc/nginx/conf.d/default.conf:ro
    networks:
      - ticket-network

networks:
  ticket-network:
    external: true