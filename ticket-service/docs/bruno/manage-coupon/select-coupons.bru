meta {
  name: Select Coupons
  type: http
  seq: 1
}

post {
  url: {{ticketBaseUrl}}/manage/coupon/select
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "page": 0,
    "size": 10,
    "sort": [
      "couponId-desc"
    ]
  }
}
