meta {
  name: Select Events
  type: http
  seq: 1
}

get {
  url: {{ticketBaseUrl}}/event/select?page=0&size=10&sort=eventId-desc
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
