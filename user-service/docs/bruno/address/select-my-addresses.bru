meta {
  name: Select My Addresses
  type: http
  seq: 2
}

post {
  url: {{userBaseUrl}}/address/select/me
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "page": 0,
    "size": 10,
    "sort": [
      "addressId-desc"
    ]
  }
}
