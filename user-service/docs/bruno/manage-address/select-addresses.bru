meta {
  name: Select Addresses
  type: http
  seq: 1
}

post {
  url: {{userBaseUrl}}/manage/address/select
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "page": 0,
    "size": 10,
    "sort": [
      "addressId-desc"
    ]
  }
}
